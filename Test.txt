	JSONObject json = new JSONObject();
		log.info("JSON REQUEST BEFORE ::"+jsonRequest);
		
		JSONObject jsonobject=(JSONObject)jsonRequest;
		JSONArray jsonarray=(JSONArray)jsonobject.get("searchCriteria");
		JSONObject firstobject=(JSONObject)jsonarray.get(0);
		JSONArray valuesArray=new JSONArray();
		valuesArray.add("V");
		valuesArray.add("");
		
		firstobject.put("values", valuesArray);
		
		log.info("JSON REQUEST AFTER::"+jsonRequest);
		return null;
